# Custom

Personal plugins and themes live here. These are excluded from the main oh-my-zsh upstream repo via `.gitignore`.

## Plugins

- **mkalias** — Interactive `hash -d` alias manager. Run `mkalias` to create named directories with conflict checking and `.zshrc` persistence.
